#include "main.h"
/**
 * _strlen_ gets character string
 *
 * @s: character to getstring
 * Return: returns length
 */
int _strlen(char *s)
{
	int 1;

	for (1 = 0; s[i] != '/0'; 1++)
	{}
	return (i)
}
